// `&&` short-circuit must skip `(one / zero)` when the LHS is false.

fn main() -> i32 {
    let zero: i32 = 0;
    let one: i32 = 1;
    if (zero != 0 && (one / zero) > one) {
        return 1;
    } else {
        return 42;
    }
}
